mod minimum_pair_removal;
use minimum_pair_removal::minimum_pair_removal;

// use median_and_mode::median;
// use median_and_mode::mode;

// use reverse_string::reverse_string;
// use anagram::anagram;
// use disemvowel::disemvowel;
// use roman_numerals::num_as_roman;

// use crate::largest_num::largest_num;
// use duplicate_encoder::duplicate_encode;

/* use validate_pin::validate_pin; */

// use love_vs_friendship::words_to_marks;
// use petals::how_much_i_love_you;
// use same::same;
/* use greet::greet; */
// use summer::two_sum;
// use duplicate::contains_duplicate;
// use is_anagram::is_anagram;
// use spin_words::spin_words;

// use longest_common_prefix::longest_common_prefix;

// use my_atoi::my_atoi;
// mod my_atoi;

// mod can_place_flowers;
// use can_place_flowers::can_place_flowers;

// mod increasing_triplets;
// use increasing_triplets::increasing_triplet;

// mod compress;
// use compress::compress;

// use minimum

// mod move_zeroes;
// use move_zeroes::move_zeroes;
/* mod prime_number; */
/* mod factorial; */
// mod reverse_string;
// mod anagram;
// mod disemvowel;
// pub mod largest_num;
// mod roman_numerals;
/* mod count_bits; */
/* mod validate_pin; */
// mod duplicate_encoder;
/* mod love_vs_friendship; */
// mod median_and_mode;
/* mod pyg_latin; */

// mod petals;
// mod same;
// mod greet;
// mod summer;
// mod duplicate;
// mod is_anagram;
// mod product_triple;
// mod score;
// mod sort_array;
// mod spin_words;

fn main() {
    // prime_number::is_prime_number(1);
    // prime_number::is_prime_number(30);

    // println!("{}", factorial::factorial(4));
    // println!("{}", factorial::factorial(1));
    // println!("{}", factorial::factorial(50));

    // reverse_string("Hello world!!");
    // reverse_string("Let's go home, yes?");
    //
    // anagram(&String::from("Madam"));
    // anagram(&String::from("Able was I 'ere I saw Elba"));

    // println!("{}", disemvowel("This website is for losers LOL!"));
    // let mut even_median = vec![1, 2, 3, 4, 5];
    // let mut odd_numbers = vec![1, 2, 3, 4, 5, 6];
    // println!("{}", median(&mut even_median));
    // println!("{}", median(&mut odd_numbers));
    //
    // let mode1 = vec![1, 1, 1, 2, 2, 2, 3, 3, 4, 4, 5];
    // let mode2 = vec![1, 2, 3, 3, 3, 3, 4, 4, 5, 5];
    // println!("{:?}", mode(&mode1));
    // println!("{:?}", mode(&mode2));

    // println!("{:?}", num_as_roman(123));

    // println!("largest number is {}", largest_num(&vec![1, 5, 2, 8, 0]));
    // println!(
    //     "largest number is {}",
    //     largest_num(&vec![2000, 5, 102, 80, 100000])
    // );
    // println!(
    //     "largest number is {}",
    //     largest_num(&vec![10, 50, 20, 8, 40])
    // );
    // println!(
    //     "largest number is {}",
    //     largest_num(&vec![11, 15, 12, 18, 0])
    // );
    // println!("The number of ones(1) is {}", count_bits::count_bit(6));
    // println!("The number of ones(1) is {}", count_bits::count_bit(1234));
    // println!("Valid pin: {}", validate_pin("1234"));
    // println!("Valid pin: {}", validate_pin("123456"));
    // println!("Valid pin: {}", validate_pin("12345"));
    // println!("Valid pin: {}", validate_pin("a234"));
    // println!("Valid pin: {}", validate_pin(""));
    // println!("Valid pin: {}", validate_pin("12.0.9"));
    // println!("Valid pin: {}", validate_pin("+111"));

    // println!("encoded string is {}", duplicate_encode("din"));
    // println!("encoded string is {}", duplicate_encode("Success"));
    // println!("encoded string is {}", duplicate_encode("recede"));
    // println!("encoded string is {}", duplicate_encode("(( @"));

    // println!("words to marks {}", words_to_marks("love"));
    // println!("words to marks {}", words_to_marks("attitude"));
    // println!("words to marks {}", words_to_marks("friendship"));
    // println!("words to marks {}", words_to_marks("knowledge"));

    // println!("{} petals is: {}", 8, how_much_i_love_you(8));
    // println!("{} petals is: {}", 30, how_much_i_love_you(30));
    // println!("{} petals is: {}", 1, how_much_i_love_you(1));
    // println!("{} petals is: {}", 3, how_much_i_love_you(3));
    // println!("{} petals is: {}", 7, how_much_i_love_you(7));
    // println!("{} petals is: {}", 6, how_much_i_love_you(6));
    // println!("{} petals is: {}", 5, how_much_i_love_you(5));

    // println!(
    //     "same array: {}",
    //     same(&mut vec![1, 2, 3], &mut vec![4, 1, 9])
    // );
    // println!("same array: {}", same(&mut vec![1, 2, 3], &mut vec![1, 9]));
    // println!(
    //     "same array: {}",
    //     same(&mut vec![1, 2, 1], &mut vec![4, 1, 4])
    // );
    // println!(
    //     "same array: {}",
    //     same(&mut vec![1, 2, 1], &mut vec![4, 1, 1])
    // );
    // println!(
    //     "same array: {}",
    //     same(&mut vec![0, 0, 0], &mut vec![0, 0, 0])
    // );

    // println!("{}", greet("Jessica"));
    // println!("{}", greet("messica"));
    // println!("{}", greet("AARON"));

    // println!("{:?}", two_sum(&[1, 2, 3], 4));
    // println!("{:?}", two_sum(&[1234, 5678, 9012], 14690));
    // println!("{:?}", two_sum(&[2, 2, 3], 4));

    // println!("{}", contains_duplicate(vec![1, 2, 3, 1]));
    // println!("{}", contains_duplicate(vec![1,2,3,4]));
    // println!("{}", contains_duplicate(vec![1,1,1,3,3,4,3,2,4,2]));

    // println!("{}", is_anagram(String::from("anagram"), String::from("baker")));
    // println!("{}", is_anagram(String::from("anagram"), String::from("margana")));
    // println!("{}", is_anagram(String::from("bale"), String::from("able")));
    // println!("{}", is_anagram(String::from("bele"), String::from("able")));

    // println!("{}", spin_words("Hey fellow warriors"));

    // println!("{}", my_atoi("42".to_string()));
    // println!("{}", my_atoi("+42".to_string()));
    // println!("{}", my_atoi(" -042".to_string()));
    // println!("{}", my_atoi("1337c0d3".to_string()));
    // println!("0-1 {}", my_atoi("0-1".to_string()));
    // println!("long ass number: {}", my_atoi(String::from("1234567890123456789012345678901234567890")));
    // println!("{}", my_atoi("-5-".to_string()));
    // println!("{}", my_atoi("3.145678".to_string()));
    // println!("{}", my_atoi("words and 987".to_string()));
    // println!("{}", my_atoi("-91283472332".to_string()));

    // println!("{}", can_place_flowers(vec![1, 0, 0, 0, 1], 1));
    // println!("{}", can_place_flowers(vec![1, 0, 0, 0, 1], 2));
    // println!("{}", can_place_flowers(vec![0, 1, 0, 1, 0], 2));

    // println!("{longest_common_prefix(vec!["flower".to_string(), "flow".to_string(),
    // "flight".to_string()])}:#?")
    println!("{}", minimum_pair_removal(vec![1, 2, 3, 1, 1, 3]));
    println!("{}", minimum_pair_removal(vec![1, 2, 3, 4, 4, 3]));
    println!("{minimum_pair_removal(vec![5 ,2, 4])}:#?");
    println!("{minimum_pair_removal(vec![1, 2, 2])}:#?");
}
